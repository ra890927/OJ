有數量限制背包問題，一開始會想到 dp[i][j] = max(dp[i - 1][j], dp[i][j - k * w[i]] + k * v[i])，
不過效率太差，對於dp裡面的數值為還剩多少 v[i]，如果在 i - 1 之前能成功完成 j 的話，那就還剩下m[i]個
v[i]，如果在第 i 個 j - v[i] 還剩下至少一個 v[i]，那就能成功完成dp[i][j]，所以剩下的就是不可能的組合。
